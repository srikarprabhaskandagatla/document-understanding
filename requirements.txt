# PyTorch 2.4+ required by transformers>=4.45 (LlavaNext image_token support)
torch==2.4.1
torchvision==0.19.1

# 4.45+ required for LlavaNextProcessor image_token argument
transformers==4.45.2

# PEFT 0.13 compatible with transformers 4.45 + torch 2.4
peft==0.13.2

# Handles device placement and mixed precision for multi-GPU/CPU offloading
accelerate==1.0.1

# bitsandbytes 0.44 adds torch 2.4 + CUDA 12.4 support
bitsandbytes==0.44.1

# Experiment tracking
wandb==0.18.7

# Levenshtein distance for ANLS metric
python-Levenshtein==0.25.0

# Config and utilities
python-dotenv==1.0.1
pyyaml==6.0.1
pillow==10.3.0
safetensors==0.4.5
numpy<2
